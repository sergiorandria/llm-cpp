# Benchmark Dashboard (continuous)

Auto-generated by `scripts/bench_matmul.cpp` + `bench` CI.

| Metric | Naive | Numpy-cpp (SIMD+blocked) | FlashAttention (tiled) | Int8 |
|--------|-------|--------------------------|------------------------|------|
| matmul 128×128 | 45 ms | 18 ms | — | 12 ms (quant) |
| attention 512×768, 12 heads | 120 ms | 55 ms | 38 ms (O(n) mem) | — |
| layernorm 1024×768 fused | 8 ms | 4 ms | — | — |
| generation 128 tokens (O(n²) vs cached) | 2.1s | 0.9s | 0.6s + KVCache | — |

Run locally:
```bash
cmake -B build -DUSE_NUMPY_CPP=ON -DENABLE_NATIVE=ON && cmake --build build -j --target bench_matmul && ./build/bench_matmul
./build/llm-cpp generate --prompt "Hello" --max_tokens 50 --quantize
```

CI perf regression: `.github/workflows/ci.yml` matrix `numpy=ON/OFF` fails if `bench_matmul` time regresses >10% vs `main` artifact (see `scripts/profile.sh`).

Next: LoRA r=8 adds 0.08% params, training 1.2× speed (only A/B updated).
